# AvComp — 项目备忘与路线图

本文档记录产品/工程审核结论与后续实现意向（随迭代更新）。

---

## 一、现状摘要

- **前台**：首页、任务、对比、收藏、个人资料、库存详情等；Firebase 登录与用户文档。
- **Market DB（`/admin/current-market`）**：静态 JSON（`marketDatabase.json`）驱动列表、筛选（Apply 后生效）、URL 状态、排序、CSV 导出、规格对比、打印/PDF（浏览器打印）、Summary 弹窗。
- **工程**：Next.js、TypeScript；`npm run build` / ESLint；暂无自动化测试脚本。

---

## 二、产品侧待办（优先级参考）

| 优先级 | 主题 | 说明 |
|--------|------|------|
| P0 | Admin 访问控制 | `/admin/*` 建议 middleware + 允许名单/角色，避免敏感挂牌数据仅靠链接保密。 |
| P0 | 数据更新闭环 | Excel → 校验 → 发布 JSON 的流程文档化；导入前后行数/字段校验脚本。 |
| P1 | 字段映射（Avionics / Galley 等） | 原始表头映射到结构化字段，表格与 Summary 才能摆脱大量「—」。 |
| P1 | 核心路径测试 | 筛选、Apply、URL 还原、导出、Summary 数值（至少 smoke / e2e）。 |
| P2 | README / 运维说明 | 环境变量、数据更新、部署与权限约定（当前 README 仍为模板）。 |
| P2 | 品牌化 PDF | 若对外交付，再评估替代 `window.print()` 的服务端或模板 PDF。 |

### 数据与指标说明

- Summary 中「机队占比」类指标若使用导出总行数作分母，与真实全球机队可能不一致；若业务需要真实机队占比，需引入**基准机队数**或审批数据源。
- 市场数据当前为**前端打包 JSON**，非 Firestore 实时库；运营侧需明确更新频率与责任人。

---

## 三、工程与交付债

- **测试**：缺少 unit/e2e；筛选 + URL + 导出链路建议至少自动化冒烟。
- **可观测性**：生产环境错误上报、关键操作日志（按需引入）。
- **CI**：建议在合并前执行 `lint` + `build`（可在 GitHub Actions 等配置）。

---

## 四、已刻意简化的功能（记录）

- 列显示/隐藏（Columns）已移除，表格固定展示全部列。
- Summary 中部分统计依赖导出字段；Avionics/Galley 等在数据源未映射前显示占位。

---

## 五、文档修订记录

- 初次写入：项目经理/开发审核结论归档；后续功能以 PR/提交说明为准。
- Market DB：YOD / Ask / DOM 筛选已改为 **双柄区间滑块**（`nouislider`），与 URL、`Apply`、query tags 行为保持不变；滑柄拖到整段刻度等同于「不设该维过滤」。
